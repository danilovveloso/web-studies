<%= form_with(model: article, id: dom_id(article)) do |form| %>
<% if article.errors.any? %>
   <div id="error_explanation">
     <h2><%= pluralize(article.errors.count, "error") %> prohibited this tweet from being saved:</h2>
​
     <ul>
      <% article.errors.each do |error| %>
         <li><%= error.full_message %></li>
      <% end %>
     </ul>
   </div>
<% end %>
​
 <div class="field">
  <%= form.label :body %>
  <%= form.text_area :body %>
 </div>
​
 <div class="actions">
  <%= form.submit %>
 </div>
<% end %>
